# Let standalone iwd configure addresses for Wi-Fi interfaces. NetworkManager
# continues to configure Ethernet independently.
[General]
EnableNetworkConfiguration=true

[Network]
NameResolvingService=systemd
